// input.js
/*
this is an example input script.
run this in a separate terminal window and make sure the receiver is on the same port
*/

const {Sender} = require("../diagnostics")
const sender = new Sender("test")

// sender.start()

// sender.send({test: "booper"})